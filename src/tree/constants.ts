import { prefix } from '../config';

export const TREE_NAME = `${prefix}-tree`;
export const TREE_NODE_NAME = `${prefix}-tree-node`;

export const FX = {
  treeNode: `${prefix}-tree-toggle`,
};

export const CLASS_NAMES = {
  tree: `${prefix}-tree`,
  treeFx: `${prefix}-tree--fx`,
  treeList: `${prefix}-tree__list`,
  treeNode: `${prefix}-tree__item`,
  treeNodeOpen: `${prefix}-tree__item--open`,
  treeNodeHidden: `${prefix}-tree__item--hidden`,
  treeNodeActive: `${prefix}-is-active`,
  disabled: `${prefix}-is-disabled`,
  hoverable: `${prefix}-tree--hoverable`,
  label: `${prefix}-tree__label`,
  icon: `${prefix}-icon`,
  treeIcon: `${prefix}-tree__icon`,
  treeIconRight: `${prefix}-icon-arrow-right`,
  treeIconDown: `${prefix}-icon-arrow-down`,
};
